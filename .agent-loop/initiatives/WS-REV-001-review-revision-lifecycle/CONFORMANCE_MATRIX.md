# Conformance Matrix: WS-REV-001

This matrix binds revised archival specification sections 25.1-25.9 to the
active reconciled contract, implementation chunks, executable proof, and final
live evidence. Chunk activation must replace proposed test names with exact
collected node IDs; a section is not complete based only on prose.

| Spec section | Owning chunks | Required executable proof | Live/evidence proof |
|---|---|---|---|
| 25.1 Authority | 05-08, 09A, 10-13 | Exactly one registered ActionId and canonical typed scope for every endpoint/command; human-only actor-kind DB enforcement; exact service ActorProfile/system-principal assignments for timer, reconciliation, artifact-reference and projection commands; chain read limited to exact submitter, active reviewer, currently granted prior participating reviewer, or privileged inspector; contributor/reviewer/both/admin/external matrices; arbitrary same-project and cross-project denial; self/creator/assignee, suspension, revocation-during-lease; two-phase evidence reauthorization leaves no canonical binding/relation/lifecycle mutation; human token isolation | Claim, context, finding/response evidence, decision, override, correction, force-release, closure, reconciliation, and replay with ActionId-bearing AuthorizationDecision links plus exact REV activation manifest proof |
| 25.2 Queue routing | 03, 05-06, 09B, 11-12 | First-open, revision-preferred, immutable exact successful admitting-CheckerRun anchor, admission-versus-supersession both race orders, duplicate delivery, active preference conflict, expiry/decline/invalidation age preservation, admin time-limited assignment, preferred-before-open, current returns one/lease/none, arbitrary or stale ID denied | Existing `review_pending` activation scan, legacy remediation report, preferred return/takeover, admin assignment, operational counts/ages |
| 25.3 Leases | 03, 06, 11 | One lease per queue and canonical human `ActorProfile.id`; frozen WS-CON reviewer compensation version independent of guide context; release/expiry distinction, database time, decision/expiry and revocation races in independent sessions/barriers, both permutations, rollback injection | Claim/release/expiry/reclaim/revocation with worker and lazy recovery enabled |
| 25.4 Reviews | 04, 08, 10 | One immutable Review/version; canonical human reviewer identity; lease/queue linkage; finding rules; reject reason; actor/operation/lease/submission/payload idempotency; replay disclosure authorization; exact immutable Review/Submission/lease/assignment/actor/verified-packet-digest/compensation-source contribution lineage and rollback | needs_revision, accept, reject, changed replay, revoked replay, crossed-lineage rejection, and contribution/outbox atomicity |
| 25.5 Revisions | 02, 04, 09A-09B, 13 | Immutable same-task N-1 Submission chain; REV-13's non-forgeable `NOT VALID` route/schema cutover preserves legacy revisions but requires preparation for all new revisions; immutable per-project guide activation sequence records chronology while drafts remain unsequenced; equal identity/sequence keeps, any different current active guide rebases with forward/backward direction, and only inconsistent/incomplete/unsafe context blocks; every preparation episode is database-bound to its exact needs_revision Review/prior Submission/task/reviewed-assignment/project and forms one non-branching immediate successor chain; normal target assignment is the reviewed assignment, while AUTH-13 authority-loss replacement atomically appends a successor targeting the replacement without rewriting source lineage; preparation freezes exact guide/source/task-execution-policy IDs, versions, hashes, and sequence but no payment/compensation context; Submission N+1 and CheckerRun use that context rather than the original task lock; target TaskAssignment and ReviewLease compensation freezes do not drift; Task Context uses the head without live-pointer drift or fallback; invalidation requires re-preparation; no reviewer guide/rebase; every unresolved blocking finding response; immutable resolutions; unresolved accept denied; full chain traversal | v1 guide context -> needs_revision -> same/forward/backward/unsafe classification -> authority-loss-only replacement transfer when required -> frozen prepared v2 context without compensation drift -> contributor context read -> checker readmission -> reviewer consumes v2 stamped context -> resolution -> accept |
| 25.6 Reject | 08, 10 | Queue close, lease consume, same-task assignment block, task `closed/review_rejected`, grant unchanged, other tasks unaffected, later version denied, reviewer contribution only | Authorized reject plus API/database/audit/CON agreement |
| 25.7 Recovery | 06, 09A, 11, 12A | Idempotent preference/lease sweeps and lazy repair; every actor/grant/self-review/policy invalidation; reason-bound covered Project Manager preparation-successor repair with stale-head/race proof; separate covered Project Manager D6 closure only after server-proven limit/deadline, with Operator/cross-project/not-reached denial; leased-without-active and consumed-without-Review detection; canonical reconciliation identity/generation with one unresolved partial uniqueness, duplicate/concurrent scan reload, one resolution, and post-resolution recurrence; Operator-only evidence-linked closure for legacy needs_revision without Review/root; no synthetic Review or silent immutable rewrite; persisted joint lifecycle phases, shared/exclusive advisory-lock mutation fencing, bounded drains, callbacks-through-drain, crash resume, and forward-only timeout recovery | Worker loss/restart, revocation, preparation repair, D6 closure/replay/races, duplicate reconciliation, legacy unrecoverable closure, controlled recurrence, reconciliation alert, durable fence/drain/crash-resume/forward-reactivation procedure |
| 25.8 Artifact evidence | 05-09B, 11 | Active-lease retrieval is limited to the canonical current packet anchored to the exact Submission: submission, admitting checker-run, current response-evidence, and required locked-context bindings; chain history is metadata-only for other versions; expired/consumed/prior/later/sibling/cross-task/project access fails; exact finding/response intake scope; two-phase ART candidate/finalize reauthorization and orphan retention under lease/grant/assignment/preparation races; raw locator rejection; preclaim unavailability creates no lease; active-lease outage creates no Review; quarantine/integrity/uncertainty fail closed; LocalStorage and MinIO/AWS-S3-compatible port conformance | Current review-packet retrieval, prior-history visibility without bytes, finding/response ART intake, orphan cleanup, outage, digest mismatch quarantine, ART-owned operator recovery through `ArtifactOperatorRecoveryPort`, no adverse outcome; Flow Node wording explicitly retired from active contract |
| 25.9 Projection | 04, 08, 12 | One canonical shared-outbox event atomically with Review; no remote call in transaction; deterministic bytes; idempotent receipt; changed bytes conflict; retry/dead-letter/reconciliation; reauthorized disclosure | Forced projection failure/retry, one ART receipt, unchanged Review, bounded outbox/dead-letter evidence |

## Closure rule

Chunk 13 validates every row against fresh real-PostgreSQL coverage evidence and
the privacy-safe HTTP drill. Missing node IDs, skipped mandatory cases, direct
database state fabrication, or an unavailable AUTH/ART/CON/outbox participant
blocks activation.
