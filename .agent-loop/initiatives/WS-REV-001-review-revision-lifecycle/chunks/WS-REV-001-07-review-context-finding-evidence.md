# Chunk Contract: WS-REV-001-07 - Review Context And Finding Evidence

## Status

Non-executable split record.

## Children

- `WS-REV-001-07A` owns active-exact-lease packet content and relationship-
  scoped immutable chain metadata after ART merges packet-read ownership.
- `WS-REV-001-07B` owns reviewer finding-evidence candidate/finalize only after
  ART merges the evidence port and AUTH merges the exact binding contracts.

Revision response evidence belongs only to 09A3. No 07 child receives raw
ArtifactStore/provider/repository access or makes history bytes readable.

## Stop condition

Use `CHUNK_MAP.md`; do not execute this parent.
